<form method="post" action="{{ path('categoriem_delete', {'id': categoriem.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categoriem.id) }}">
    <button class="btn">Delete</button>
</form>
